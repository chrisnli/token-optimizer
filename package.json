{
  "name": "smartcodex",
  "version": "0.1.0",
  "description": "Codex CLI harness with classifier-driven automatic model selection.",
  "type": "module",
  "bin": {
    "smartcodex": "./bin/smartcodex.js",
    "smartcodex-classify": "./bin/smartcodex-classify.js"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node -e \"const{execFileSync}=require('node:child_process');const{readdirSync}=require('node:fs');const files=[...readdirSync('bin').filter(f=>f.endsWith('.js')).map(f=>'bin/'+f),...readdirSync('src').filter(f=>f.endsWith('.js')).map(f=>'src/'+f),...readdirSync('test').filter(f=>f.endsWith('.test.js')).map(f=>'test/'+f)];for(const f of files){execFileSync(process.execPath,['--check',f],{stdio:'inherit'})}\""
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true
}
