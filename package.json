{
  "name": "smartcodex-classify",
  "version": "0.1.0",
  "description": "Classifier-only Codex routing MVP.",
  "type": "module",
  "bin": {
    "smartcodex-classify": "./bin/smartcodex-classify.js"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node --check bin/smartcodex-classify.js && node --check src/*.js && node --check test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true
}
